# Application Configuration
spring.application.name=Smart Campus Operations Hub
server.port=8081

# MongoDB Configuration
spring.mongodb.uri=${MONGODB_URI:mongodb+srv://${MONGODB_USERNAME:akila}:${MONGODB_PASSWORD:5GqojY60uiYeEreU}@cluster0.ckomb02.mongodb.net/${MONGODB_DATABASE:paftanya}?retryWrites=true&w=majority}

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173
app.booking.verification-base-url=${BOOKING_VERIFICATION_BASE_URL:http://localhost:8081/api/bookings}

# ==============================================
# EMAIL CONFIGURATION - FIXED VERSION
# ==============================================

# Option 1: STARTTLS on Port 587 (Default)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:hansana687@gmail.com}
spring.mail.password=${MAIL_PASSWORD:cymmtqigcktqvxdg}
spring.mail.protocol=smtp
spring.mail.default-encoding=UTF-8

# SMTP Properties for Port 587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# Trust Gmail's certificate (Fixes SSL issues)
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Debug email issues (Remove after fixing)
spring.mail.properties.mail.debug=true

# From Address Configuration
app.mail.from=${MAIL_FROM:hansana687@gmail.com}
app.mail.from-name=${MAIL_FROM_NAME:Smart Campus Team}